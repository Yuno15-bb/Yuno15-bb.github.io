---
titre: "Field Operations Platform"
pitch: "Installable web app for a carpentry and roofing company: the technician records the job on the roof, the office sees it live, and everything works with no signal."
audience: ["business"]
contexte: "Client work · anonymized"
role: "Product design, field UX, full-stack development"
stack: ["PWA", "JavaScript", "Service Worker", "Supabase", "PostgreSQL", "RLS", "GPS", "PDF", "Python", "FastAPI", "Claude", "Gmail", "Microsoft Graph"]
annee: 2026
resultat: "Full offline capture, replayed on reconnection with no duplicates. The office sees the job the same day instead of three days later."
nda: true
featured: true
ordre: 1
cover: "/projects/terrain-tel-04-09.jpg"
cover2: "/projects/terrain-bureau-04-09.jpg"
---

import Figure from '../../../components/Figure.astro';
import Flow from '../../../components/figures/Flow.astro';
import Ecrans from '../../../components/figures/Ecrans.astro';
import Duo from '../../../components/figures/Duo.astro';
import Mac from '../../../components/figures/Mac.astro';
import FicheProposition from '../../../components/figures/FicheProposition.astro';

<Figure
  cadre={false}
  source="Screenshots of the app in production, demo mode — branding, places and data neutralised"
>
  <Ecrans
    tel="/projects/terrain-tel-04-09.jpg"
    telAlt="The technician's phone — current job, route, upcoming visits"
    bureau="/projects/terrain-bureau-04-09.jpg"
    bureauAlt="The office — live crews on the map, counters for the day, month planner"
  />
</Figure>

<Figure
  cadre={false}
  legende="The same product in light theme — the technician switches depending on whether he is on a roof in full sun or back in the van."
>
  <Ecrans
    clair
    tel="/projects/terrain-tel-clair-04-09.jpg"
    telAlt="The phone, light theme"
    bureau="/projects/terrain-bureau-clair-04-09.jpg"
    bureauAlt="The office, light theme"
  />
</Figure>

## What it is for

Replacing the sheet of paper a roofer fills in on the roof and somebody re-types at the
office three days later. One application, two uses: the technician's phone, and the
office dashboard.

## On the roof — the technician's phone

- **Questionnaire targeted by job type** — survey, leak detection, maintenance contract,
  intervention. Fifteen steps at most, and only the ones relevant to that job.
- **Multi-select findings** across the five zones of a roof — ridge, verges, covering,
  flashing, penetrations. One point can carry several defects; "good condition"
  automatically excludes any other.
- **Photos taken inside the app**, one per subject, geotagged and attached to the right
  step on their own.
- **Annotatable roof sketch**, with an outline editor to correct a footprint.
- **Today's job, route and upcoming visits** on the home screen.

## At the office — a single page

- **Live crew map**: each technician's GPS position, distinguishing a current fix from a
  last known position, with pins coloured by job status.
- **Counters for the day**: total jobs, technicians online, jobs in progress, urgent
  findings.
- **Month planner**, showing the most serious status of each day.
- **Location directory** in three levels: managing agent, site, job.
- **Client report as PDF**, produced in one gesture from what was entered on the roof —
  no re-typing.
- **Built-in assistant**: the same company runs an agent that reads its inbox and
  prepares entries; it is reachable from this page.

## The agent that prepares the records

The office does not only receive what the field types in. Signed quotes, invoices and
complaints arrive by email, and someone used to re-type them by hand into the client
records. An agent reads those inboxes, opens the attachments and prepares the record; the
person keeps the decision.

<Figure
  cadre={false}
  legende="The review screen: every field was extracted from the email, every field stays editable, and the button on the right is what triggers the write. In amber, the one field corrected by hand."
  source="Reconstructed interface — fictional data (client confidentiality)"
>
  <Mac titre="Field Ops — Filing">
    <FicheProposition />
  </Mac>
</Figure>

- **Two inboxes, polled every minute** — Gmail and Microsoft Graph.
- **Classification by type**: signed quote, client invoice, subcontractor invoice,
  complaint, payment notice, spam.
- **PDF attachment extraction**: client, quote number, invoice number, amounts excluding
  and including tax, address, due date, occupant of the property.
- **A scanned invoice** goes through character recognition; one email may carry both the
  quote and the purchase order, and both follow the record all the way through.
- **Three destination pages, fifty-five columns, thirty-five filled on their own.**

<Figure
  titre="The boundary, spelled out"
  legende="The single write exception is narrow and written down in plain sight: one status field may move to « scheduled », nothing else."
>
  <Duo
    a={{ titre: 'What the agent does on its own', items: [
      'Read the inboxes and classify every message',
      'Open PDF attachments and extract their data',
      "Query the client's database — read-only",
      'Draft a reply, left unsent',
    ] }}
    b={{ titre: 'What it is not allowed to do', items: [
      "Write into the client's live database",
      'Send an email on their behalf',
      'Create a record nobody has re-read',
      'Delete or overwrite existing data',
    ] }}
  />
</Figure>

<Figure
  titre="From inbox to filed record"
  legende="No arrow connects the agent to the client's records: everything goes through the queue and through one person's click."
>
  <Flow etapes={[
    { t: 'The inbox', d: 'Quotes, invoices and complaints arrive mixed together.' },
    { t: 'The agent reads', d: 'It classifies, opens the PDFs, extracts the data.' },
    { t: 'The proposal queue', d: 'Every write stops here and waits.' },
    { t: 'A person approves', d: 'They re-read it, correct it, and their click triggers the write.', porte: true },
    { t: 'The client records', d: 'The record is created, its PDFs attached. This is the only path.' },
  ]} />
</Figure>

## Offline: an outage is the normal case

<Figure
  titre="What happens when the signal drops"
  legende="No entry depends on the signal at the moment it is made."
>
  <Flow etapes={[
    { t: 'The technician confirms', d: 'He fills in, photographs, confirms.' },
    { t: 'Written on the phone', d: 'Immediately, asking the server for nothing.', porte: true },
    { t: 'The signal returns', d: 'The queue replays on its own, in order, no duplicates.' },
    { t: 'The office sees it', d: 'The same job, from the other side.' },
  ]} />
</Figure>

Every write is stored locally and queued before it ever approaches the network. A
refresh, a closed tab, a basement: nothing is lost, and the same item replayed twice does
not create two records.

## Operations

- **Per-company isolation** at the database level, designed in from the start rather than
  bolted on later.
- **Encrypted off-machine backups**, with restoration verified byte for byte.
- **Installable from the browser**, with no app store in the way.
- **Keyboard operable**, contrast meeting the AA threshold — an app that has to be read
  in full sunlight.
