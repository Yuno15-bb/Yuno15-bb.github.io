---
titre: "GreyMatter"
pitch: "Persistent memory for command-line agents: an installable engine, kept separate from your notes, that updates itself. Published as open source."
audience: ["business"]
contexte: "Personal project · open source"
role: "Design, architecture, extraction, publication"
stack: ["Python", "Bash", "Electron", "WebGL", "GitHub Actions", "Apache 2.0"]
annee: 2026
resultat: "46 releases published under Apache 2.0, installable in one command — and a leak check that is allowed to block any publication."
cover: "/projects/gmtr-ipad-24-09.webp"
coverEntier: true
featured: false
ordre: 2
---

import Figure from '../../../components/Figure.astro';
import Flow from '../../../components/figures/Flow.astro';
import Duo from '../../../components/figures/Duo.astro';
import MacBureau from '../../../components/figures/MacBureau.astro';
import Apercu from '../../../components/figures/Apercu.astro';

<Figure
  cadre={false}
  legende="While an agent works, a pill in the menu bar names it and says what it is doing; below, the same corner enlarged. Open it and the steps of the run line up underneath; each kind of work has its own colour, up to “Done”. Together with the map, it is the only interface: everything else is text."
  source="Filmed on this Mac, in demonstration mode: English interface, invented code inside the orb. Only the corner of the screen where the pill lives is kept; the enlargement is added in the edit."
>
  <Apercu
    src="/projects/gmtr-pastille-25-09.mp4"
    poster="/projects/gmtr-pastille-25-09.webp"
    largeur={1434}
    hauteur={884}
    description="The right-hand corner of a Mac menu bar, at night. Top right, the GreyMatter pill with its panel open underneath; below, the same corner enlarged. Torrens tidies the map, Anesidora writes the digest, Torrens commits; the glass orb changes colour with the kind of work. At the end, “Done” and three steps."
  />
</Figure>

## What it is for

A command-line agent is brilliant inside a session and amnesiac between two. GreyMatter
gives it memory that lasts: whatever is solved once is distilled into a note, filed,
linked, and found again in the next session.

## What the engine does

- **Distillation**: a working session becomes clean notes — one fact per note, with its
  provenance and its degree of certainty.
- **Recall on demand**: ask — a keyword, a sentence or a command — and the notes that
  answer come back in under 50 milliseconds across a thousand notes. Nothing enters the
  conversation unless it was asked for.
- **Knowledge tree**: notes link to each other; a lesson learned on one project is
  findable from every other.
- **Four maintenance ships**: eight narrow missions — distil, file, challenge, link,
  archive, repair, watch the machine, synthesise — carried by four agents named after the
  ships of *Alien*. Each mission writes only in its own zone.
- **The map and the pill**: the whole trunk as a cloud of points you can walk through,
  and a pill in the menu bar that says which agent is working, and on what.

The public package is at version 1.28: recall there is still automatic and maintenance
still carries eight role names. The pill, the ships and recall on demand ship with the
next release.

## How a session becomes memory

<Figure
  titre="Everything runs on the machine — nothing here is a service you log into"
  legende="There is no orchestrator: each stage hangs off a moment in the session. That is what makes the memory free to use — you never have to remember to feed it."
>
  <Flow etapes={[
    { t: 'The trunk', d: 'Plain Markdown files on the machine: the notes, the map, the state.' },
    { t: 'When you ask', d: 'A lexical search picks the few notes that answer, and returns them into the conversation.' },
    { t: 'During the session', d: 'What is written and what is read are recorded — the real material of the work.' },
    { t: 'At session end', d: 'The session is archived, then distilled into notes and filed. Another mission wakes only if its sensor crosses a threshold — at most one per pass.', porte: true },
  ]} />
</Figure>

## Opening

<Figure
  cadre={false}
  legende="On opening, the GMTR window settles on the desktop and its logo builds itself while the trunk loads."
  source="Filmed on 24/09 on this Mac's full screen, window at its true size. Official Apple frame."
>
  <MacBureau
    src="/projects/gmtr-ouverture-24-09.mp4"
    poster="/projects/gmtr-ouverture-24-09.webp"
    description="A Mac desktop at sunset. A small glass window appears in the middle; three white bars stack inside it, then turn into the GMTR logo, and a loading bar fills underneath."
  />
</Figure>

## What measurement changed

- **Automatic recall was switched off.** Until 9 September, the notes judged relevant were
  pasted into every question unasked. Over thirty days, only 4% were then opened — 139 out
  of 3,256. The search stayed, the unrequested suggestion went; one setting brings it back
  without touching the code.
- **The pill became light.** At rest it took 28% of a processor core, and another 29% from
  the program that draws the screen: every frame per second was paid for twice. Each
  element now animates at the pace of what visibly moves in it, and nothing draws while
  hidden — at rest, 4% and 20%.

## What makes publication possible

<Figure
  titre="Two folders that never mix"
  legende="The split is not a filing convention: an update replaces the engine wholesale, and is not allowed to write into the notes."
>
  <Duo
    a={{ titre: 'The engine — disposable, replaced on every update', items: [
      'The code that files, links and retrieves',
      'The checks and the measurement benches',
      'The install and migration tooling',
    ] }}
    b={{ titre: 'The trunk — it only moves when I write', items: [
      'Four months of notes, lessons and decisions',
      'Client names, addresses, machine paths',
      'What an update must never touch',
    ] }}
  />
</Figure>

## The chain that protects the lived record

<Figure
  titre="From the living notebook to the public package"
  legende="A one-way chain: nothing flows back from the public repository into the notebook. The leak check rereads the repository's whole history, not just the latest version, and publication leaves only when it is green."
>
  <Flow etapes={[
    { t: 'The living notebook', d: 'Four months of real notes: clients, addresses, machine paths.' },
    { t: 'Allow-list copy', d: 'What is not named does not leave.' },
    { t: 'De-identification', d: 'Replayable rules strip names, numbers and paths.' },
    { t: 'Leak check', d: '21 markers. One of them turning red blocks the publication.', porte: true },
    { t: 'The public package', d: 'The engine, installable by anyone. Never the lived record.' },
  ]} />
</Figure>

## How it installs and stays alive

- **One command**: the package installs as an extension to a command-line agent.
- **Every release is replayed** by continuous integration: full install, uninstall, and
  every past migration run **twice** — with the requirement that it has eaten no note.
- **46 releases published**, under the Apache 2.0 licence, on a public repository.

Source code: [github.com/Yuno15-bb/GreyMatter-engine](https://github.com/Yuno15-bb/GreyMatter-engine)
