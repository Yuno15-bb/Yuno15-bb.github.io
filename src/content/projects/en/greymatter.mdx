---
titre: "GreyMatter"
pitch: "Persistent memory for command-line agents: an installable engine, kept separate from your notes, that updates itself. Published as open source."
audience: ["business"]
contexte: "Personal project · open source"
role: "Design, architecture, extraction, publication"
stack: ["Python", "Bash", "Electron", "WebGL", "GitHub Actions", "Apache 2.0"]
annee: 2026
resultat: "46 releases published under Apache 2.0, installable in one command — and a leak check that is allowed to block any publication."
cover: "/projects/gmtr-principes-mac-25-09.webp"
coverEntier: true
featured: false
ordre: 1
gabarit: produit
ton: jour
---


import Ouverture from '../../../components/produit/Ouverture.astro';
import Preuves from '../../../components/produit/Preuves.astro';
import Section from '../../../components/produit/Section.astro';
import Moment from '../../../components/produit/Moment.astro';
import Capacites from '../../../components/produit/Capacites.astro';
import AvantApres from '../../../components/produit/AvantApres.astro';
import Installer from '../../../components/produit/Installer.astro';
import Fabrication from '../../../components/produit/Fabrication.astro';
import Figure from '../../../components/Figure.astro';
import Flow from '../../../components/figures/Flow.astro';
import MacBureau from '../../../components/figures/MacBureau.astro';
import Apercu from '../../../components/figures/Apercu.astro';

export const depot = 'https://github.com/Yuno15-bb/GreyMatter-engine';

<Ouverture
  logo={{ src: '/projects/gmtr-icone-22-09.png', alt: "GreyMatter app icon" }}
  annonce="V2 release soon"
  reperes={['Open source', 'macOS']}
  titre="Persistent memory for Claude Code."
  chapo="GreyMatter turns every session into linked Markdown notes, versioned with git, that your agent finds again from one session to the next."
  actions={[
    { label: 'View on GitHub', url: depot, plein: true },
    { label: 'Install', url: '#installer' },
  ]}
  pile={frontmatter.stack}
  pileTitre="Stack"
  legende="The map: every dot is a note, grouped by region. Click a region to expand it, a note to read its text and links. Demo filmed on a sample repository at real density, with no names."
>
  <Apercu
    src="/projects/gmtr-carte-25-09.mp4"
    poster="/projects/gmtr-carte-25-09.webp"
    largeur={1600}
    hauteur={1088}
    description="The glass GMTR window against a sunset sky. First the whole map: the regions as small clusters, each with its number of notes. We enter claude-brain, which unfolds into a sphere; two notes are hovered and their preview appears on the right, then one is opened: “Green sabotage has three possible causes”. The GRAPH view spreads its links out to the other regions. Back to the map, then the principles region, where “Check at primary source, never assume” opens. Along the bottom, all the time: the regions by size, the log of what is read and written, and the code being written."
  />
</Ouverture>

<Preuves items={[
  { valeur: '46 releases', sens: 'published under the Apache 2.0 licence, on a public repository.' },
  { valeur: '< 50 ms', sens: 'per search across a thousand notes.' },
  { valeur: '100% local', sens: 'no account, no server: files on your machine.' },
  { valeur: '21 markers', sens: 'checked for leaks before every release. A single hit blocks it.' },
]} />

<Section
  surtitre="Interfaces"
  titre="Two views on your memory."
  chapo="Everything else is Markdown: you read it, edit it and version it like code."
>
  <Moment surtitre="Launch" titre="The map rebuilds from your files.">
    <p slot="texte">The logo assembles while the notes load. The map is recomputed on every launch, so it always reflects the actual state of the repository.</p>
    <MacBureau
      src="/projects/gmtr-ouverture-24-09.mp4"
      poster="/projects/gmtr-ouverture-24-09.webp"
      description="A Mac desktop at sunset. A small glass window appears in the middle; three white bars stack inside it, then turn into the GMTR logo, and a loading bar fills underneath."
    />
  </Moment>
  <Moment surtitre="Menu bar" titre="Watch your agents work, live." badge="V2 · soon">
    <p slot="texte">Each step of the task shows up under the pill, colour-coded by type of work, until “Done”. At idle it uses 4% of one CPU core.</p>
    <Apercu
      src="/projects/gmtr-pastille-25-09.mp4"
      poster="/projects/gmtr-pastille-25-09.webp"
      largeur={1434}
      hauteur={884}
      description="The right-hand corner of a Mac menu bar, at night. Top right, the GreyMatter pill with its panel open underneath; below, the same corner enlarged. Torrens tidies the map, Anesidora writes the digest, Torrens commits; the glass orb changes colour with the kind of work. At the end, “Done” and three steps."
    />
  </Moment>
</Section>

<Section
  surtitre="Features"
  titre="Stop re-explaining things to your agent."
  chapo="Claude Code starts from zero every session. GreyMatter keeps what was learned and makes it available across all your projects."
>
  <Capacites items={[
    { titre: 'Distillation', texte: 'At the end of a session, the transcript is split into notes: one fact per note, with its source and confidence level.' },
    { titre: 'Search on demand', texte: 'A keyword, a sentence or brain recall: the relevant notes land in context. Nothing gets in unless you asked for it.', badge: 'V2 · soon · 1.28 injects automatically' },
    { titre: 'Notes linked across projects', texte: 'A lesson learned on one project is available in every other. The bigger the repository, the sharper the answers.' },
    { titre: '8 maintenance agents', texte: 'Distil, file, challenge, link, archive, repair, watch the machine, synthesise. Each agent can only write to its own zone.', badge: 'V2 · soon · grouped into 4 ships' },
    { titre: 'Freshness review', texte: 'A note that was never re-checked enters a review queue, dated from the git history.' },
    { titre: 'Updates with rollback', texte: 'The engine replaces itself on every session, and brain update --rollback returns to the previous version. Your notes are never modified.' },
  ]} />
</Section>

<Section
  surtitre="Architecture"
  titre="Built on Claude Code hooks."
  chapo="No account, no remote service: every step fires on a session event."
>
  <Figure>
    <Flow etapes={[
      { t: 'The repository', d: 'Markdown files versioned with git: notes, map, state.' },
      { t: 'On demand', d: 'A lexical search selects the relevant notes and injects them into the conversation.' },
      { t: 'During the session', d: 'Every file read or written is logged.' },
      { t: 'Session end', d: 'The transcript is archived, distilled into notes, then filed. A maintenance agent only starts if its sensor crosses a threshold, one per pass.', porte: true },
    ]} />
  </Figure>
</Section>

<Section
  surtitre="Product decisions"
  titre="Two features, revisited after measuring."
  chapo="If a feature isn't used, it goes, even when it was the original idea."
>
  <AvantApres
    libelles={{ avant: 'Before', apres: 'After' }}
    items={[
      { titre: 'Automatic injection', avant: '4%', avantSens: 'of the notes injected unasked were actually opened: 139 out of 3,256, over 30 days.', apres: 'On demand', apresSens: 'Search stays, unsolicited injection is gone. One setting turns it back on without touching the code.' },
      { titre: 'Pill at idle', avant: '28% + 29%', avantSens: 'of one CPU core: the pill, then the macOS process that draws the screen. Every frame was paid for twice.', apres: '4% + 20%', apresSens: 'Each area animates at the rate of what actually moves, and nothing renders while the window is hidden.' },
    ]}
  />
</Section>

<Installer
  titre="Install in two commands."
  chapo="GreyMatter installs as a Claude Code plugin, then updates itself every session."
  lignes={['/plugin marketplace add Yuno15-bb/GreyMatter-engine', '/plugin install c-brain@c-brain']}
  copier="Copy"
  copie="Copied"
  actions={[
    { label: 'Source code on GitHub', url: depot, plein: true },
    { label: 'Full install guide', url: `${depot}/blob/main/INSTALL.md` },
  ]}
  note="Every release goes through CI: install, uninstall and a replay of every migration, twice, with zero lost notes required. Public version: 1.28. The pill, the 4 ships and search on demand ship in the next one."
/>

<Fabrication items={[
  { terme: 'Role', valeur: frontmatter.role },
  { terme: 'Context', valeur: `${frontmatter.contexte} · ${frontmatter.annee}` },
  { terme: 'Result', valeur: frontmatter.resultat },
]} />
